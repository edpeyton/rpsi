# rpsi (development version)

# rpsi 1.0.0

* Initial commit and submission to CRAN.

* `psi(...)` is the main wrapper function.

* `plot(<rpsi>)` is the main method for visualizing object of class `rpsi`.

